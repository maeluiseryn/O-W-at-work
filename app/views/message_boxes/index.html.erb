<h1>Listing message_boxes</h1>

<table>
  <tr>
    <th>Description</th>
    <th>Box type</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @message_boxes.each do |message_box| %>
  <tr>
    <td><%= message_box.description %></td>
    <td><%= message_box.box_type %></td>
    <td><%= link_to 'Show', message_box %></td>
    <td><%= link_to 'Edit', edit_message_box_path(message_box) %></td>
    <td><%= link_to 'Destroy', message_box, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Nouvelle Boite a Message', new_message_box_path %>
